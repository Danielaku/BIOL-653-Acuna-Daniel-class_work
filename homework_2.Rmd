---
title: "Homework 2"
author: "Daniel Acuna"
date: "September 30, 2015"
output: html_document
---

```{r, echo=FALSE}
library(gapminder)
str(gapminder)
install.packages('ggplot2')
library('ggplot2')
```

Plot 1

```{r}
ggplot(data = gapminder, aes(x = gdpPercap, y = lifeExp, 
                             colour = continent)) + 
  geom_point(aes(shape = continent))
```
I can't make the graph display the diferent geometric shapes....


Plot 2
log(gdpPercap

```{r}
ggplot(data = gapminder, aes(x = log(gdpPercap), y = lifeExp,
                             colour = continent)) + 
  geom_point(aes(shape = continent))
```

Plot 3
```{r}
ggplot(data = gapminder, aes(x = log(gdpPercap), y = lifeExp,
                             colour = continent)) + 
  geom_point(aes(shape = continent)) +
  geom_abline()
```
